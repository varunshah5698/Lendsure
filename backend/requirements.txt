fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
python-multipart>=0.0.9
# ML inference (risk_model.joblib). Pinned-free on purpose; any recent 1.x works.
# Without these the engines transparently fall back to rules.
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3
